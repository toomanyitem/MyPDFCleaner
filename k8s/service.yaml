apiVersion: v1
kind: Service
metadata:
  name: mypdfcleaner-service
spec:
  type: NodePort
  selector:
    app: mypdfcleaner
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
