# 📄 MyPDFCleaner – ระบบเลือกไฟล์แล้วลบข้อความจาก PDF อัตโนมัติ

MyPDFCleaner คือสคริปต์ Python สำหรับลบข้อความออกจาก PDF ได้หลายรายการในครั้งเดียว  
สามารถพิมพ์ข้อความทีละบรรทัดผ่าน Terminal แล้วกด Enter / พิมพ์ `done` เพื่อเริ่มทำงาน  
รองรับ PDF ทุกหน้า ใช้ Redaction (ลบทิ้งจริง) ไม่ใช่แค่ซ่อนข้อความ

คุณสามารถ **เลือกชื่อไฟล์ PDF เองได้ตอนรัน**  
รวมถึงตั้งชื่อ Output เองได้ ไม่ถูกบังคับใช้ไฟล์ default

เหมาะกับงานลบ watermark, โลโก้เดิม, ข้อความเครดิต, ข้อความบริษัท ฯลฯ  
และสร้างไฟล์ใหม่ให้โดยไม่ยุ่งกับไฟล์ต้นฉบับ

---

## 🚀 ความสามารถหลัก

| ฟีเจอร์ | รายละเอียด |
|---|---|
| ลบข้อความหลายคำในครั้งเดียว | ใส่ข้อความทีละบรรทัดได้ไม่จำกัด |
| เลือกไฟล์ PDF เองได้ | ไม่จำเป็นต้องใช้ชื่อไฟล์ตายตัว |
| ตั้งชื่อไฟล์ Output เองได้ | กำหนดไฟล์ใหม่เองทุกครั้ง |
| ใช้ Redaction | ข้อความหายจริงไม่เหลือใน Content |
| Setup อัตโนมัติด้วย `setup.sh` | ติดตั้งเร็ว ใช้ได้ทันที |
| ปลอดภัย ไม่ยุ่งกับต้นฉบับ | บันทึกแยกเป็นไฟล์ใหม่เสมอ |

---

## 📥 ความต้องการ

- Python 3.8+
- macOS / Linux (Windows ใช้ผ่าน WSL ได้)
- มีสิทธิ์รัน shell script (`chmod +x setup.sh`)

ตรวจสอบ Python:

```bash
python3 --version
```

---

## 🟩 Getting Started / วิธีใช้งาน

1) ดาวน์โหลดโปรเจกต์

```bash
git clone https://github.com/toomanyitem/MyPDFCleaner.git
cd MyPDFCleaner
```

---

2) Setup อัตโนมัติ (ติดตั้ง + สร้างไฟล์สคริปต์ให้เอง)

```bash
chmod +x setup.sh
./setup.sh
```

โปรแกรมจะสร้าง:

- `venv/`
- `remove_text.py`

---

3) วางไฟล์ PDF ที่ต้องการในโฟลเดอร์นี้

📌 ไม่จำเป็นต้องตั้งชื่อเฉพาะอีกแล้ว คุณสามารถเลือกไฟล์ได้ตอนรันโปรแกรม

โครงสร้างโปรเจกต์หลัง Setup:

```
MyPDFCleaner/
 ├── README.md
 ├── setup.sh
 ├── remove_text.py
 ├── venv/
 └── YourFile.pdf        ← เพียงวางไว้ที่นี่
```

---

4) เริ่มใช้งาน

```bash
source venv/bin/activate
python remove_text.py
```

โปรแกรมจะถาม:

```
ชื่อไฟล์ PDF ที่ต้องการลบข้อความ:  invoice.pdf
ชื่อไฟล์ผลลัพธ์ใหม่ (default=cleaned_output.pdf):  invoice_CLEAN.pdf

ข้อความที่ต้องการลบทีละรายการ:
ข้อความ: confidential
ข้อความ: powered by xxx
ข้อความ: done
```

แล้วระบบจะสร้างไฟล์ใหม่ให้ทันที 💾

```
invoice_CLEAN.pdf
```

---

## 📊 ตัวอย่างผลลัพธ์การทำงาน

```
================ RESULT ================
🔍 พบข้อความรวม: 12 ครั้ง

 • 'confidential' → 5 จุด
 • 'powered by xxx' → 7 จุด

📁 ไฟล์บันทึกแล้ว: invoice_CLEAN.pdf
========================================
```

---

## ⚙️ การปรับแต่งเพิ่มเติม

หากต้องการตั้งค่า default เอง → เปิด `remove_text.py`

```python
input_file = input("-- PDF input filename: ").strip()
output_file = input("-- Output filename: ").strip()
```

สามารถใช้ร่วมกับระบบในอนาคต เช่น:

| เพิ่มเติมที่ต้องการ | วิธีทำ |
|---|---|
| รองรับ text input จาก .txt | เพิ่ม file import |
| ลบหลายไฟล์ batch mode | loop รายการ PDF |
| เพิ่ม GUI drag & drop | พร้อมออกแบบต่อให้ได้ |

---

## 🧰 Troubleshooting

| ปัญหา | วิธีแก้ |
|---|---|
| รันแล้วไม่ลบอะไรเลย | PDF อาจเป็นรูปภาพ ต้อง OCR ก่อน |
| บางคำหาไม่เจอ | คำอาจถูกแยกเป็น H u n a n → ใส่คำย่อแทน |
| No module named fitz | รัน `source venv/bin/activate && pip install pymupdf` |
| Permission denied ./setup.sh | ให้สิทธิ์ก่อน → `chmod +x setup.sh` |

---

ปิด venv

```bash
deactivate
```

---

## Roadmap (แผนพัฒนาถัดไป)

- GUI เลือกไฟล์แบบคลิกได้ (ไม่ต้องพิมพ์)
- Drag & Drop เพื่อลบได้ทันที
- Batch PDF (หลายไฟล์พร้อมกัน)
- text-list import (.txt / .json)
- รองรับ command flag เช่น --input --output

---

End of README.md ✔
