📄 MyPDFCleaner – ระบบลบข้อความออกจาก PDF อัตโนมัติ

MyPDFCleaner คือสคริปต์ Python สำหรับลบข้อความออกจากไฟล์ PDF ได้หลายรายการในครั้งเดียว
สามารถป้อนข้อความทีละบรรทัดผ่าน Terminal แล้วพิมพ์ done เพื่อเริ่มทำงาน
รองรับ PDF ทุกหน้า ใช้การ Redaction (ปิดทับจริง) ไม่ใช่แค่ซ่อนข้อความ

เหมาะกับงานลบ watermark / โลโก้ / คำอธิบายเดิมออกจาก PDF
ไม่กระทบไฟล์ต้นฉบับ และสร้างไฟล์ PDF ใหม่ให้แทน

⸻

🚀 ความสามารถหลัก

ฟีเจอร์	รายละเอียด
ลบข้อความได้หลายคำพร้อมกัน	กรอกข้อความทีละบรรทัดไม่จำกัด
ใช้เทคนิค Redaction	ข้อความถูกลบทิ้งจริง ไม่มีเหลือใน content layer
แสดงผลสรุปจำนวนที่ลบได้	เห็นจำนวนที่พบในแต่ละหน้าและรวมทั้งหมด
ติดตั้งง่ายด้วย setup.sh	Setup อัตโนมัติในคำสั่งเดียว
ปลอดภัย ไม่ยุ่งกับไฟล์ต้นฉบับ	ผลลัพธ์อยู่ในไฟล์ใหม่ .cleaned


⸻

📥 ความต้องการ
	•	Python 3.8 ขึ้นไป
	•	ระบบรองรับ macOS / Linux
	•	หากเป็น Windows สามารถรันผ่าน Git Bash / WSL ได้

ตรวจสอบ Python ก่อนเริ่ม:

python3 --version


⸻

🟩 Getting Started / วิธีใช้งาน

1) ดาวน์โหลดโปรเจ็ค

git clone https://github.com/toomanyitem/MyPDFCleaner.git
cd MyPDFCleaner

(เปลี่ยน your-username ตาม GitHub ของคุณ)

⸻

2) รันคำสั่ง Setup เพื่อติดตั้งอัตโนมัติ

สคริปต์ setup.sh จะ:

| สร้าง venv | ✔
| ติดตั้ง pymupdf | ✔
| สร้าง remove_text.py ให้อัตโนมัติ | ✔

chmod +x setup.sh
./setup.sh


⸻

3) นำไฟล์ PDF ของคุณมาวางในโฟลเดอร์

ตั้งชื่อไฟล์ว่า:

xy_vending_command.pdf

ตัวอย่างโครงสร้างโฟลเดอร์:

MyPDFCleaner/
 ├── setup.sh
 ├── README.md
 ├── remove_text.py
 ├── venv/
 └── xy_vending_command.pdf  ⬅ ต้องใส่ไฟล์นี้เอง


⸻

4) เปิดใช้งาน Virtual Environment

source venv/bin/activate     # macOS / Linux


⸻

5) เริ่มลบข้อความ

python remove_text.py

โปรแกรมจะให้ป้อนข้อความทีละบรรทัด เช่น:

Text to remove: Hunan Xing Yuan Technology Co., Ltd
Text to remove: XY Vending
Text to remove: www.xy-vending.com
Text to remove: done   ← พิมพ์หรือกด Enter เปล่า

💾 แล้วไฟล์ผลลัพธ์จะถูกสร้างชื่อว่า:

xy_vending_command_cleaned.pdf


⸻

📊 ตัวอย่างผลลัพธ์ที่ได้

================ RESULT ================
🔍 ลบข้อความทั้งหมด: 12

  • 'Hunan Xing Yuan Technology Co., Ltd' → พบ 4 ครั้ง
  • 'XY Vending' → พบ 3 ครั้ง
  • 'www.xy-vending.com' → พบ 5 ครั้ง

📁 บันทึกไฟล์ใหม่: xy_vending_command_cleaned.pdf
========================================


⸻

⚙️ การปรับแต่งเพิ่มเติม

เปิดไฟล์ remove_text.py แก้ตรงนี้:

input_file = "xy_vending_command.pdf"
output_file = "xy_vending_command_cleaned.pdf"

คุณสามารถปรับเป็นรูปแบบอื่นได้ เช่น:

ต้องการ	แก้เป็น
เปลี่ยนชื่อไฟล์ input	เปลี่ยน input_file
ให้ผู้ใช้เลือกรูปแบบ output เอง	รองรับเพิ่ม argument
ลบ Text จากไฟล์หลาย PDF	เพิ่ม loop รายชื่อไฟล์


⸻

🧰 แก้ปัญหา (Troubleshooting)

ปัญหา	วิธีแก้
รันแล้วไม่พบข้อความ	ไฟล์เป็น PDF แบบรูปภาพ ต้อง OCR ก่อน
ลบบางคำไม่หมด	คำอาจแยกเป็นตัว เช่น H u n a n → ให้ใช้คำย่อแทน
ขึ้น error No module named fitz	ยังไม่ activate venv → source venv/bin/activate
Permission denied ตอนรัน setup.sh	แก้ด้วย → chmod +x setup.sh


⸻

🧿 ผู้ใช้เสร็จงานแล้ว สามารถปิด venv ได้ด้วย:

deactivate


⸻

📌 Roadmap เวอร์ชันถัดไป
	•	รองรับ Drag & Drop GUI
	•	batch process PDF หลายไฟล์พร้อมกัน
	•	Import text-to-remove จาก .txt / .json
	•	รองรับ argument CLI เช่น --input --output --textfile

⸻
