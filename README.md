📄 MyPDFCleaner

ระบบสำหรับ ลบข้อความหลายรายการออกจาก PDF ได้พร้อมกัน
เหมาะสำหรับงานลบ watermark / brand / credit หรือ string ที่ไม่ต้องการใน PDF
รองรับการกรอกข้อความผ่าน Terminal ทีละบรรทัดจนกว่าผู้ใช้จะพิมพ์ done / จบ เพื่อเริ่มทำงาน

⸻

🔧 วิธีติดตั้งแบบทำตามทีละขั้นตอน (พร้อมใช้งานจริง)

⛔ ต้องมี Python 3 อยู่แล้ว
ถ้ายังไม่มี → ติดตั้งจาก python.org ก่อน

⸻

1) เปิด Terminal แล้วรันตามนี้ทีละบรรทัด

# 1. สร้างโฟลเดอร์โปรเจค และเข้าไปข้างใน
mkdir MyPDFCleaner && cd MyPDFCleaner

# 2. สร้าง Virtual Environment
python3 -m venv venv

# 3. เปิดใช้งาน Virtual Environment
source venv/bin/activate      # ⬅ Mac / Linux
# Windows →  venv\Scripts\activate

# 4. ติดตั้งไลบรารีที่จำเป็น
pip install pymupdf

# 5. สร้างไฟล์ remove_text.py ด้วยคำสั่งเดียว (copy ได้เลย)
cat << 'EOF' > remove_text.py
import fitz  # PyMuPDF

def remove_texts_from_pdf(input_pdf_path, output_pdf_path, texts_to_remove):
    if not texts_to_remove:
        print("❗ ไม่มีข้อความสำหรับลบ — ยกเลิกการทำงาน")
        return

    try:
        doc = fitz.open(input_pdf_path)

        found_counts = {text: 0 for text in texts_to_remove}
        total_found = 0

        for page in doc:
            has_redactions = False
            for text in texts_to_remove:
                areas = page.search_for(text)
                if areas:
                    found_counts[text] += len(areas)
                    total_found += len(areas)
                    has_redactions = True
                    for a in areas:
                        page.add_redact_annot(a)

            if has_redactions:
                page.apply_redactions(images=0, graphics=0)

        doc.save(output_pdf_path)
        doc.close()

        print("\n================ RESULT ================")
        print(f"🔍 ลบข้อความทั้งหมด {total_found} ครั้ง\n")
        for t, c in found_counts.items():
            print(f"   • {t}  →  {c} จุด")
        print("\n📁 ไฟล์ถูกบันทึกแล้ว →", output_file)
        print("=========================================\n")

    except Exception as e:
        print(f"เกิดข้อผิดพลาด: {e}")

def ask_texts():
    print("\nพิมพ์ข้อความที่ต้องการลบทีละบรรทัด")
    print("กด ENTER ว่าง หรือพิมพ์ done/จบ เพื่อเริ่มทำงาน\n")

    items = []
    while True:
        t = input("ข้อความ: ").strip()
        if t == "" or t.lower() in ["done","จบ","q","exit"]:
            break
        items.append(t)
    return items

if __name__ == "__main__":
    input_file  = "xy_vending_command.pdf"
    output_file = "xy_vending_command_cleaned.pdf"

    to_delete = ask_texts()
    remove_texts_from_pdf(input_file, output_file, to_delete)
EOF


⸻

📂 โฟลเดอร์หลังติดตั้งเสร็จ

MyPDFCleaner/
 ├── venv/
 ├── remove_text.py   ✔ (สร้างอัตโนมัติ)
 └── xy_vending_command.pdf   ← คุณต้องนำใส่เอง


⸻

▶ วิธีใช้งาน
	1.	วาง PDF ที่ต้องการไว้ในโฟลเดอร์
ชื่อไฟล์ต้องเป็น:

xy_vending_command.pdf

	2.	รันโปรแกรม

python remove_text.py

	3.	ใส่ข้อความที่จะลบทีละบรรทัด เช่น:

ข้อความ: Hunan Xing Yuan Technology Co., Ltd
ข้อความ: XY Vending
ข้อความ: www.xy-vending.com
ข้อความ: done

	4.	ไฟล์ผลลัพธ์จะได้เป็น

xy_vending_command_cleaned.pdf


⸻

🔥 ตัวอย่าง Output จริง

================ RESULT ================
🔍 ลบข้อความทั้งหมด 12 ครั้ง

   • Hunan Xing Yuan Technology Co., Ltd → 4 จุด
   • XY Vending → 3 จุด
   • www.xy-vending.com → 5 จุด

📁 ไฟล์ถูกบันทึกแล้ว → xy_vending_command_cleaned.pdf
=======================================


⸻

🧰 Troubleshooting

ปัญหา	คำอธิบาย / วิธีแก้
รันแล้วไม่มีอะไรถูกลบ	PDF อาจเป็นภาพ สแกน → ต้อง OCR ก่อน
ลบได้บางคำไม่ครบทุกหน้า	คำอาจสะกดแตก → ลองพิมพ์บางส่วนแทนคำเต็ม
Error: No module named fitz	ลืม activate venv → source venv/bin/activate แล้ว pip install pymupdf
อยากเลือกไฟล์อื่น	เปลี่ยนชื่อไฟล์ใน remove_text.py ตรง input_file


⸻

🧨 ปิด Virtual Environment (เมื่อใช้งานเสร็จ)

deactivate


⸻

📌 อัปเกรดในอนาคต

☑ GUI Version ใช้แบบคลิกแทน terminal
☑ batch ลบไฟล์หลาย PDF พร้อมกัน
☑ import รายชื่อคำที่ต้องการลบจาก .txt
